class Auto:
    def scraping():
        urls= config.urls

        for url in urls:
            scr = Scraping(url,今か昔,開始時間)
            contents[]
            contents.append(scr)
        
        thread = contents.sort()

        return thread

    def sort():
        thread  = this.contents.sort
        return thread